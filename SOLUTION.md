# Explicit reference solution

- This branch is **solution**, not the learner starter. Main remains deliberately 501.
- Search implements the complete approved contract; existing acceptance assertions are unchanged.
- Additional acceptance case: unknown repeated keys do not alter q/limit matching, sorting or total.
- Validate this checkout with `npm ci` then `npm run verify:solution`.
- Do not use `verify:baseline`: its starter-only 501 assertion is intentionally inapplicable to a complete feature.
- This reference demonstrates code correctness, **not** that a Copilot client loaded the toolkit or implemented it.
- The customization curriculum and real client usage evidence remain learner activities.
- Inspect only after a genuine attempt or deliberate walkthrough choice; never merge this branch into main.
