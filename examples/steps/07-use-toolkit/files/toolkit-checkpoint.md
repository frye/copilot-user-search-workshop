# Toolkit checkpoint — NOT a search implementation

- Author prerequisites: reviewed 01 instructions, 02 prompt, 03 canonical skill, 04 package metadata, 05 MCP-aware source/version update.
- Build **1.1.0** from canonical author source, inspect checksum and version.
- Consumer: clean sibling from starter; retained instruction/prompt content; `.github/skills` absent.
- Register exactly one built package, inspect real loaded source/version; do not copy project skill.
- Consumer 05: configure two fixture MCP tools or explicitly label fallback. Retain actual source/version evidence.
- Consumer 06: inspect role tool permissions; planner → human approval → one implementer → reviewer.
- Confirm search still returns 501 before the attempt. This checkpoint changes no API/test implementation.
- Invoke installed workflow with approved task, review plan, approve focused search/test changes, add a meaningful case.
- Run regression/full acceptance and preserve outputs. Fix defects, never weaken tests.
- Record client/ref/package/tool evidence and remaining gaps; specimen checklist alone is not usage proof.
