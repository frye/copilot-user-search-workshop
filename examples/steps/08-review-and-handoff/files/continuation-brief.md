# Continuation brief — replace fields with real observations

- Goal / current stage:
- Consumer root / branch / `git rev-parse HEAD`:
- Current client / exact build:
- Approved contract path and decisions:
- Human approval / owned files:
- Changed files and diff summary:
- Exact checks / exits / relevant output:
- Intentional failures versus unresolved defects:
- Installed toolkit source / version / checksum:
- Local skill absent and duplicate registrations checked:
- MCP tool source / fixture version / observed call or explicit fallback:
- Role / actual permissions / advisory-only limitations:
- Supported review findings and fixes:
- Author canonical source revision / package update status:
- Remaining work and next safe action:
- New session: re-read contract, source/ref, current diff and package/tool evidence; do not assume this brief is current without checking.
